{
  "name": "ai-company-lib",
  "version": "1.0.0",
  "description": "Core libraries for AI Agent Company — embeddings, semantic search, browser automation",
  "type": "module",
  "scripts": {
    "browser-server": "node browser-server.js",
    "test-embeddings": "node -e \"import('./embeddings.js').then(m => m.embed('test')).then(v => console.log('Embedding dims:', v.length))\""
  },
  "dependencies": {
    "@xenova/transformers": "^2.17.0",
    "puppeteer": "^22.0.0",
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
